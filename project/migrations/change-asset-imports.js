/**
 * This migration updates the references inside of frontend/src/css to static asset files,
 * and changes them from being relative references to taking advantage of webpack's module resolution capabilities.
 */
